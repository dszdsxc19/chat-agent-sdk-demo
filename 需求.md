我需要一个 SDK，充当高性能的共享总线。宿主把‘Zod 实例’和‘函数闭包’挂载上去，Web Component 里的 React 组件能原封不动地把它们取下来。UI 利用 Zod 实例的结构自动渲染界面，并利用函数闭包在用户操作时触发宿主的业务逻辑。

场景：宿主注册工具，UI 利用 Assistant-ui 的 makeAssistantTool 注册，并且使用 ai-sdk 发送到后端 agent。在ai-sdk 要调用工具的时候，能够通过 SDK 调用到 宿主，并且执行工具，最后结果能够让UI获取到。

