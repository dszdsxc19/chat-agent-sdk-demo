// Export the React component if needed
export { ToolWidget } from './ToolWidget';
// Export the Custom Element class
export { AgentWidgetElement } from './element';
// Import the element to ensure side-effects (registration) happen
import './element';
